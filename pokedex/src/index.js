import React from 'react';
import ReactDOM from 'react-dom/client';
import Login from './Login';

const root = ReactDOM.createRoot(document.getElementById('root'));

// Render the Login component into the root element.
root.render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>
);
